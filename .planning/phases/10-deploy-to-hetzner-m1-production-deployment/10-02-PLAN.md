---
phase: 10-deploy-to-hetzner-m1-production-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/scripts/bootstrap.ts
  - apps/api/src/scripts/__tests__/bootstrap.test.ts
  - apps/api/package.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running bootstrap creates the initial owner account from ADMIN_EMAIL/ADMIN_PASSWORD"
    - "Re-running bootstrap is a no-op when the owner already exists (idempotent, exit 0)"
    - "Bootstrap applies the DB schema (drizzle-kit push) before creating the owner"
  artifacts:
    - path: "apps/api/src/scripts/bootstrap.ts"
      provides: "Idempotent first-run: schema push + owner creation"
      contains: "createOwnerIfMissing"
    - path: "apps/api/package.json"
      provides: "bootstrap script entry"
      contains: "bootstrap"
  key_links:
    - from: "apps/api/src/scripts/bootstrap.ts"
      to: "@rentular/db users table"
      via: "getDb() select-then-insert with bcrypt hash"
      pattern: "createOwnerIfMissing"
---

<objective>
Create the one-off, idempotent first-run bootstrap (D-08): apply the schema via drizzle-kit push, then create the initial owner/landlord account. No seed script exists today — this is net-new.

Purpose: A freshly deployed box has an empty database and no usable login. Bootstrap makes the app usable and is safe to re-run on every deploy.
Output: apps/api/src/scripts/bootstrap.ts + `pnpm bootstrap` script + unit test for the idempotent owner-create path.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-CONTEXT.md
@.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-RESEARCH.md

<interfaces>
<!-- Owner-create logic to mirror. Extracted from apps/api/src/routes/auth.ts:15,41-58 -->
apps/api/src/routes/auth.ts register path:
  const PASSWORD_SALT_ROUNDS = 12;
  select { id } from users where email = normalizedEmail  (skip if exists)
  const userId = crypto.randomUUID();
  const passwordHash = await bcrypt.hash(password, PASSWORD_SALT_ROUNDS);
  await db.insert(users).values({ id: userId, email, passwordHash, ... });

@rentular/db exports: getDb, users (and full schema via export * from ./schema/index)
drizzle-kit push script: packages/db → `db:push` = `drizzle-kit push`; root `pnpm db:push` = `turbo db:push`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Idempotent createOwnerIfMissing() with unit test</name>
  <files>apps/api/src/scripts/bootstrap.ts, apps/api/src/scripts/__tests__/bootstrap.test.ts</files>
  <read_first>
    - apps/api/src/routes/auth.ts (register logic: PASSWORD_SALT_ROUNDS=12, crypto.randomUUID, bcrypt.hash, users insert shape and required NOT NULL columns)
    - packages/db/src/schema/index (users table columns — confirm required fields: id, email, passwordHash, name, role/ownership)
    - apps/api/src/lib/__tests__/stripe.test.ts (created in Plan 01) for vitest stub/mock style consistency
  </read_first>
  <behavior>
    - Test 1: createOwnerIfMissing(stubDb, {email, password}) with a select returning an existing row → performs NO insert, returns a "skipped" result
    - Test 2: createOwnerIfMissing(stubDb, {email, password}) with select returning empty → calls insert exactly once with a bcrypt-hashed password (not plaintext) and a crypto.randomUUID id
    - Test 3: missing ADMIN_EMAIL or ADMIN_PASSWORD → throws a descriptive Error before any DB call
  </behavior>
  <action>
    In apps/api/src/scripts/bootstrap.ts export an async function createOwnerIfMissing(db, { email, password }) that: normalizes email (lowercase/trim per auth.ts), selects users by email, returns { created: false } if present, else bcrypt.hash(password, 12) + db.insert(users) with crypto.randomUUID() id and the minimum required NOT NULL columns (owner role/name), returns { created: true }. Keep the DB handle a parameter (not imported inside) so it is unit-testable with a stub. Add apps/api/src/scripts/__tests__/bootstrap.test.ts using a fake db object (select().from().where().limit() chain returning [] or [row]; insert().values() spy) covering the three behaviors. Log with a [Bootstrap] prefix per project convention.
  </action>
  <verify>
    <automated>pnpm --filter @rentular/api test --run src/scripts/__tests__/bootstrap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - createOwnerIfMissing exported from apps/api/src/scripts/bootstrap.ts
    - Test proves no insert when owner exists; insert-once with hashed password when missing
    - `grep -q "bcrypt" apps/api/src/scripts/bootstrap.ts` and `grep -q "randomUUID" apps/api/src/scripts/bootstrap.ts` succeed
  </acceptance_criteria>
  <done>Owner-create logic is idempotent and unit-tested against a stub db.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap entrypoint (schema push + owner) and pnpm script</name>
  <files>apps/api/src/scripts/bootstrap.ts, apps/api/package.json</files>
  <read_first>
    - packages/db/package.json (db:push = drizzle-kit push) and packages/db/drizzle.config.ts (reads DB_* from process.env)
    - root package.json scripts (turbo db:push wiring)
    - 10-RESEARCH.md § "Pitfall 5" (drizzle-kit is a devDependency; run bootstrap from the build tree / a stage that retains devDeps)
  </read_first>
  <action>
    Add a `main()` in apps/api/src/scripts/bootstrap.ts that: (1) shells out to run `drizzle-kit push` for @rentular/db (via `pnpm --filter @rentular/db exec drizzle-kit push`, inheriting DB_* from process.env), then (2) obtains getDb() and calls createOwnerIfMissing with ADMIN_EMAIL/ADMIN_PASSWORD from process.env, then exits 0. Guard main() so it only runs when the module is the entrypoint (import.meta.url check), leaving createOwnerIfMissing importable by the test. Add a `"bootstrap": "tsx src/scripts/bootstrap.ts"` script to apps/api/package.json (tsx is already a devDependency). This runs from the deploy build tree where devDeps (drizzle-kit, tsx) are present — do not add them to the prod runner image.
  </action>
  <verify>
    <automated>grep -q '"bootstrap"' apps/api/package.json && grep -q "drizzle-kit push" apps/api/src/scripts/bootstrap.ts && grep -q "import.meta.url" apps/api/src/scripts/bootstrap.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm bootstrap` script present in apps/api/package.json
    - bootstrap.ts orchestrates drizzle-kit push then createOwnerIfMissing
    - main() is guarded by an import.meta.url entrypoint check (test import does not trigger schema push)
  </acceptance_criteria>
  <done>`pnpm --filter @rentular/api bootstrap` applies schema and creates the owner idempotently; re-run is a no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy env → bootstrap | ADMIN_EMAIL/ADMIN_PASSWORD injected from /opt/rentular/.env; bootstrap writes the first credential to the DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Spoofing | Owner account creation | mitigate | Password bcrypt-hashed (rounds 12, mirrors auth.ts); never stored plaintext; unit test asserts hash used |
| T-10-02-02 | Elevation of Privilege | Re-run overwrites owner | mitigate | Idempotent select-first; existing owner is a no-op, never re-hashes or resets password |
| T-10-02-03 | Information Disclosure | ADMIN_PASSWORD in logs | mitigate | [Bootstrap] logs never echo the password value; only "owner created/exists" status |
| T-10-02-SC | Tampering | npm installs | accept | No new packages (bcrypt, tsx, drizzle-kit already present) — no legitimacy gate needed |
</threat_model>

<verification>
- `pnpm --filter @rentular/api test --run` (bootstrap test + Plan 01 tests green)
- Idempotency proven at unit level; full DB run verified live in Plan 06 (needs MySQL — no MySQL in sandbox per STATE blocker)
</verification>

<success_criteria>
- createOwnerIfMissing is idempotent and unit-tested
- `pnpm bootstrap` wired; runs schema push + owner create from the build tree
- No plaintext password persisted or logged
</success_criteria>

<output>
Create `.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-02-SUMMARY.md` when done
</output>
