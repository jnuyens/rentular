---
phase: 10-deploy-to-hetzner-m1-production-deployment
plan: 06
type: execute
wave: 5
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "https://rentular.com is reachable over HTTPS with a valid Let's Encrypt certificate"
    - "The API is healthy through nginx (/api/v1/health returns status:healthy with DB+Redis ok)"
    - "The served HTML contains zero references to localhost:4000 (build-ARG baked correctly)"
    - "The bootstrapped owner can log in; the deploy pipeline flips a new release on push"
    - "The environment unblocks 09-HUMAN-UAT (Ponto sandbox E2E + Mailpit renewal-email check)"
  artifacts: []
  key_links:
    - from: "rentular.com A record"
      to: "m1 host IP"
      via: "DNS"
      pattern: "rentular.com"
    - from: "nginx (host)"
      to: "web:3000 + api:4000/api/v1"
      via: "installed deploy/nginx/rentular.conf"
      pattern: "proxy_pass"
---

<objective>
Execute the live production deployment on the m1 host: provision, deploy, and verify the full stack is reachable at https://rentular.com, then unblock the deferred Phase 9 UAT.

Purpose: Everything upstream produced local artifacts. This plan runs them against the real box — the only place DNS, Docker, TLS, and the live provider round-trips exist. These tasks require the operator (SSH, DNS control, secrets placement) and cannot be autonomous.
Output: A live, verified production deployment; smoke checks green; 09-HUMAN-UAT unblocked.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-CONTEXT.md
@.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-VALIDATION.md
@.planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision m1 — DNS, secrets, nginx, TLS, tooling</name>
  <read_first>
    - .env.production.example (Plan 04 — the surface to populate /opt/rentular/.env)
    - deploy/nginx/rentular.conf (Plan 05 — the server block to install)
    - 10-RESEARCH.md § "Environment Availability" (verify Docker/Compose on m1) and § Assumptions A3/A7
  </read_first>
  <what-built>Local artifacts are ready: Dockerfiles, docker-compose.prod.yml, deploy.sh, webhook receiver, smoke.sh, nginx conf, prod env template.</what-built>
  <how-to-verify>
    Operator performs on m1 (Hetzner, m1.linuxbe.com, passwordless sudo):
    1. Confirm the deploy branch is `main` (Assumption A7) — correct deploy.sh/webhook if different.
    2. Point the rentular.com A record at the m1 IP; confirm `dig +short rentular.com` resolves to m1.
    3. Verify Docker Engine + Compose v2 present (`docker --version`, `docker compose version`); install via apt if missing.
    4. Create /opt/rentular/.env from .env.production.example with REAL secrets (strong DB_ROOT_PASSWORD/DB_PASSWORD, AUTH_SECRET via `openssl rand -base64 32`, DEPLOY_WEBHOOK_SECRET, PONTO sandbox creds, ADMIN_EMAIL/ADMIN_PASSWORD). `chown root:root` + `chmod 600`. Confirm it is git-ignored.
    5. Install deploy/nginx/rentular.conf into the host nginx; issue the LE cert for rentular.com via the existing automation; `nginx -t` clean; reload.
    6. Register the GitHub webhook → the m1 receiver endpoint with the same DEPLOY_WEBHOOK_SECRET.
  </how-to-verify>
  <acceptance_criteria>
    - `dig +short rentular.com` returns the m1 IP
    - /opt/rentular/.env exists, root-owned, chmod 600, populated with real (non-placeholder) secrets
    - `nginx -t` passes with rentular.conf installed; LE cert issued for rentular.com
    - Docker + Compose v2 available on m1
  </acceptance_criteria>
  <resume-signal>Type "provisioned" when DNS resolves, secrets are in place (600/root), nginx+TLS are live, and the webhook is registered.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy + bootstrap + smoke</name>
  <read_first>
    - deploy/deploy.sh, deploy/smoke.sh (Plan 05)
    - apps/api/src/scripts/bootstrap.ts (Plan 02)
  </read_first>
  <what-built>The deploy pipeline (deploy.sh + webhook) and smoke.sh from Plan 05, driving docker-compose.prod.yml.</what-built>
  <how-to-verify>
    1. Trigger a deploy: push to `main` (webhook → deploy.sh) OR run deploy/deploy.sh manually on m1 for the first release.
    2. Confirm an atomic release: $DEPLOY_ROOT/releases/<ts> created and `current` symlink points to it.
    3. Confirm containers: `docker compose -f current/docker-compose.prod.yml ps` → mariadb/redis/mailpit/api/web all healthy/running (single api replica).
    4. Confirm bootstrap ran idempotently (owner created on first run; "owner exists" no-op if re-run).
    5. Run `deploy/smoke.sh https://rentular.com` — expect all green.
  </how-to-verify>
  <verify>
    <automated>deploy/smoke.sh https://rentular.com</automated>
    <human-check>All 5 compose services healthy; releases/<ts> + current symlink present; bootstrap idempotent on re-run.</human-check>
  </verify>
  <acceptance_criteria>
    - `curl -sSf -o /dev/null -w '%{http_code}' https://rentular.com` → 200
    - `curl -sSf https://rentular.com/api/v1/health` → `"status":"healthy"` (DB+Redis ok)
    - `curl -s https://rentular.com | grep -c 'localhost:4000'` → 0
    - Valid LE cert dates via openssl; `docker compose ps` all healthy
    - Re-running bootstrap is a no-op (exit 0, owner-exists)
  </acceptance_criteria>
  <resume-signal>Type "smoke-green" when deploy/smoke.sh passes and all containers are healthy.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner login + unblock 09-HUMAN-UAT</name>
  <read_first>
    - .planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-HUMAN-UAT.md (the prod UAT this deploy unblocks)
    - 10-CONTEXT.md § D-01 (Mailpit capture on m1, :8025)
  </read_first>
  <what-built>The live, TLS-fronted Rentular stack with a bootstrapped owner account and Mailpit capture.</what-built>
  <how-to-verify>
    1. Log in at https://rentular.com with ADMIN_EMAIL/ADMIN_PASSWORD → dashboard renders (session cookie set, secure).
    2. Verify an SSR-rendered dashboard page AND a client-side XHR both hit https://rentular.com/api/v1 (DevTools Network shows no localhost:4000) — confirms no SSR-hairpin failure (Pitfall 2).
    3. Run the 09-HUMAN-UAT.md items against prod: Ponto sandbox bank-connection connect → callback E2E; trigger a renewal-warning email and confirm it is captured in Mailpit at http://<m1>:8025 (or the bound loopback) in the correct locale.
  </how-to-verify>
  <verify>
    <human-check>Owner login works; SSR + client XHR both target the prod API; Ponto sandbox E2E completes; renewal email visible in Mailpit in the user's locale.</human-check>
  </verify>
  <acceptance_criteria>
    - Owner can log in and reach the dashboard over HTTPS
    - No localhost:4000 requests in the browser Network tab (SSR + client)
    - 09-HUMAN-UAT items pass against production (Ponto sandbox E2E + Mailpit renewal email)
  </acceptance_criteria>
  <resume-signal>Type "uat-unblocked" when owner login works and the 09-HUMAN-UAT items pass against prod.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → https://rentular.com | Public ingress via nginx/TLS; app tier loopback-only |
| operator → /opt/rentular/.env | Real secrets placed on the box (root-600) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Information Disclosure | Secrets at rest on the box | mitigate | /opt/rentular/.env chown root:root + chmod 600; git-ignored; verified in Task 1 acceptance |
| T-10-06-02 | Spoofing | Invalid/self-signed TLS | mitigate | LE cert via existing m1 automation; openssl dates checked in smoke |
| T-10-06-03 | Information Disclosure | App tier reachable directly | mitigate | web/api bound 127.0.0.1; DB/Redis unpublished (Plan 04); nginx sole ingress |
| T-10-06-04 | Tampering | "deploys green, app broken" API URL | mitigate | smoke asserts zero localhost:4000 in served HTML; DevTools SSR+XHR check in Task 3 |
| T-10-06-SC | Tampering | on-box build inputs | accept | Build-on-box from verified main push (Plan 05 HMAC gate); no external registry (D-07) |
</threat_model>

<verification>
- deploy/smoke.sh https://rentular.com → all green
- Owner login + SSR/XHR both hit prod API (no localhost:4000)
- 09-HUMAN-UAT items pass against production
</verification>

<success_criteria>
- https://rentular.com live over HTTPS with valid LE cert
- API healthy through nginx; served HTML free of localhost:4000
- Owner login works; deploy pipeline flips releases on push
- 09-HUMAN-UAT unblocked and passing on prod
</success_criteria>

<output>
Create `.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-06-SUMMARY.md` when done
</output>
