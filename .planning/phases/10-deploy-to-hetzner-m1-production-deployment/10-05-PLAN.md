---
phase: 10-deploy-to-hetzner-m1-production-deployment
plan: 05
type: execute
wave: 4
depends_on: ["10-04"]
files_modified:
  - deploy/deploy.sh
  - deploy/webhook-receiver.mjs
  - deploy/webhook-receiver.test.mjs
  - deploy/rentular-webhook.service
  - deploy/smoke.sh
  - deploy/nginx/rentular.conf
autonomous: true
requirements: []
must_haves:
  truths:
    - "deploy.sh gates on `pnpm build` and NEVER on `pnpm lint` (57 pre-existing tsc errors)"
    - "deploy.sh sources /opt/rentular/.env (without printing it) and overrides DB_HOST=127.0.0.1 so the host-shell bootstrap can reach the loopback-published MariaDB"
    - "deploy.sh checks out to releases/<ts>, flips the current symlink, builds+ups compose, runs idempotent bootstrap, prunes old releases"
    - "The webhook receiver HMAC-SHA256 verifies X-Hub-Signature-256 (constant-time) and restricts to the deploy branch before exec'ing deploy.sh; it runs as a loopback systemd service on 127.0.0.1:9000"
    - "smoke.sh asserts HTTPS 200, /api/v1/health healthy, valid TLS dates, and zero localhost:4000 in served HTML"
    - "nginx routes / to web:3000, /api/v1 to api:4000 preserving the prefix, and /deploy-webhook to the loopback receiver, forwarding X-Forwarded-Proto https"
  artifacts:
    - path: "deploy/deploy.sh"
      provides: "Atomic build-on-box deploy with pnpm build gate"
      contains: "pnpm build"
    - path: "deploy/webhook-receiver.mjs"
      provides: "HMAC-verified GitHub push receiver"
      contains: "timingSafeEqual"
    - path: "deploy/rentular-webhook.service"
      provides: "systemd unit running the receiver on 127.0.0.1:9000"
      contains: "webhook-receiver.mjs"
    - path: "deploy/smoke.sh"
      provides: "Post-deploy HTTP smoke checks"
      contains: "api/v1/health"
    - path: "deploy/nginx/rentular.conf"
      provides: "Reverse-proxy server block (reference, installed on host)"
      contains: "proxy_pass"
  key_links:
    - from: "deploy/webhook-receiver.mjs"
      to: "deploy/deploy.sh"
      via: "exec only after signature + branch verify"
      pattern: "deploy.sh"
    - from: "deploy/nginx/rentular.conf"
      to: "api:4000/api/v1"
      via: "location /api/v1 proxy_pass preserving prefix"
      pattern: "/api/v1"
    - from: "deploy/nginx/rentular.conf"
      to: "127.0.0.1:9000 (receiver)"
      via: "location /deploy-webhook proxy_pass"
      pattern: "/deploy-webhook"
---

<objective>
Author the deploy pipeline artifacts: the atomic build-on-box deploy.sh, the HMAC-verified GitHub webhook receiver (+ its systemd unit), the post-deploy smoke script, and the nginx reverse-proxy server block.

Purpose: Adapt the modulejail git-push to webhook to atomic-release philosophy to the dynamic multi-process Docker case (D-05/D-07). The deploy gate is pnpm build, never lint (D-06). The webhook is an arbitrary-code-execution surface — it MUST be HMAC-authenticated and reachable only via nginx to a loopback-bound receiver.
Output: deploy/deploy.sh, deploy/webhook-receiver.mjs (+ test), deploy/rentular-webhook.service, deploy/smoke.sh, deploy/nginx/rentular.conf.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-RESEARCH.md
@.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-CONTEXT.md

<interfaces>
<!-- modulejail deploy philosophy (adapt, do not copy): -->
~/src/modulejail-website/scripts/deploy.sh: git push to https://modulejail.com/deploy-webhook.php to deploy.sh to atomic releases/<ts> + `current` symlink. (modulejail is static rsync; Rentular is multi-process Docker — build+up containers off `current`.)
Deploy branch: main (git status confirms current branch main; assumption A7 — confirm with operator in Plan 06).
Health path (VERIFIED): /api/v1/health (Hono basePath). Served publicly at https://rentular.com/api/v1/health; internally api:4000/api/v1/health.
Compose file to drive: docker-compose.prod.yml (Plan 04). Bootstrap: `pnpm --filter @rentular/api bootstrap` (Plan 02), run from the release build tree (has devDeps).
Bootstrap env (load-bearing): deploy.sh runs as a HOST shell script, so /opt/rentular/.env is NOT auto-loaded (only compose reads env_file:). The host-shell bootstrap must source the secrets itself AND use DB_HOST=127.0.0.1, because the compose service DNS `mariadb` does not resolve from the host and MariaDB is loopback-published at 127.0.0.1:3306 (Plan 04).
Receiver port (load-bearing): the webhook receiver listens on 127.0.0.1:9000; nginx proxies /deploy-webhook to it. Never bind the receiver to 0.0.0.0.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy/deploy.sh — atomic build-on-box deploy (build gate, not lint)</name>
  <files>deploy/deploy.sh</files>
  <read_first>
    - ~/src/modulejail-website/scripts/deploy.sh (the atomic-release philosophy to adapt)
    - 10-RESEARCH.md § "deploy.sh gate (D-06)" and § "Pitfall 8" (atomic symlink semantics for containers)
    - docker-compose.prod.yml (Plan 04 — the compose file deploy drives; mariadb loopback-published at 127.0.0.1:3306)
  </read_first>
  <action>
    Create deploy/deploy.sh with `set -euo pipefail`. Steps: resolve DEPLOY_ROOT (e.g. /opt/rentular), TS=$(date +%Y%m%d%H%M%S); checkout/copy the pushed tree into $DEPLOY_ROOT/releases/$TS; `pnpm install --frozen-lockfile` (installs devDeps too — the release tree therefore has drizzle-kit + tsx available for bootstrap); run `pnpm build` as the DEPLOY GATE (tsup + next build) — NEVER `pnpm lint` (57 pre-existing tsc errors would fail every deploy per D-06); atomically flip `$DEPLOY_ROOT/current` symlink to releases/$TS (ln -sfn to a temp name then mv, for atomicity); `docker compose -f current/docker-compose.prod.yml build`; `docker compose -f current/docker-compose.prod.yml up -d`. Then the one-shot idempotent bootstrap: FIRST load the secrets into the shell WITHOUT printing them — `set -a; . "$DEPLOY_ROOT/.env"; set +a` (source only; never `cat`/`echo` the file — the "never cat/echo the .env" rule means never PRINT it; sourcing is required and allowed); then OVERRIDE `export DB_HOST=127.0.0.1` for the bootstrap invocation (the compose service DNS `mariadb` does not resolve from the host shell, and MariaDB is loopback-published at 127.0.0.1:3306 by Plan 04); then run `pnpm --filter @rentular/api bootstrap` from the release tree (devDeps present — drizzle-kit push + tsx owner-create). Prune releases/ keeping the newest 5. NEVER cat/echo /opt/rentular/.env or any secret (source it, do not print it). On any failure, do not flip current (or roll back the symlink). Add a comment documenting rollback = repoint current to previous release + `up -d --build`.
  </action>
  <verify>
    <automated>bash -n deploy/deploy.sh && grep -q "pnpm build" deploy/deploy.sh && ! grep -q "pnpm lint" deploy/deploy.sh && grep -Eq "^\s*(\.|source) .*/\.env" deploy/deploy.sh && grep -q "DB_HOST=127.0.0.1" deploy/deploy.sh && ! grep -Eq 'cat .*/opt/rentular/.env|cat .*\$\{?DEPLOY_ROOT\}?/.env|echo .*SECRET' deploy/deploy.sh && grep -q "docker-compose.prod.yml" deploy/deploy.sh && grep -q "releases/" deploy/deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n deploy/deploy.sh` passes (syntax valid)
    - Contains `pnpm build`; does NOT contain `pnpm lint`
    - Sources the secrets file (`. /opt/rentular/.env` or `source`, via $DEPLOY_ROOT) before bootstrap, and sets `DB_HOST=127.0.0.1` for the bootstrap invocation
    - Never `cat`/`echo`s the env file: `! grep -Eq 'cat .*/opt/rentular/.env|echo .*SECRET' deploy.sh` holds (sourcing with `.`/`source` is allowed; printing is not)
    - States the release tree has devDeps (drizzle-kit + tsx) available via the full `pnpm install`
    - Creates releases/<ts>, flips `current` atomically, builds+ups docker-compose.prod.yml, runs bootstrap, prunes to 5
    - `set -euo pipefail` present
  </acceptance_criteria>
  <done>deploy.sh performs an atomic, build-gated, container-based release; the host-shell bootstrap sources secrets and reaches the loopback-published MariaDB via DB_HOST=127.0.0.1; secrets are never printed.</done>
</task>

<task type="auto">
  <name>Task 2: deploy/webhook-receiver.mjs — HMAC-verified push receiver (+ test + systemd unit)</name>
  <files>deploy/webhook-receiver.mjs, deploy/webhook-receiver.test.mjs, deploy/rentular-webhook.service</files>
  <read_first>
    - 10-RESEARCH.md § "GitHub webhook HMAC verification" and § Security Domain (webhook RCE mitigation)
    - deploy/deploy.sh (Task 1 — the script this receiver exec's)
  </read_first>
  <action>
    Create deploy/webhook-receiver.mjs: a minimal Node HTTP listener bound to 127.0.0.1:9000 (loopback ONLY — nginx proxies /deploy-webhook to it; never 0.0.0.0) that reads the RAW request body, computes `sha256=` + HMAC_SHA256(process.env.DEPLOY_WEBHOOK_SECRET, rawBody), and compares against the X-Hub-Signature-256 header using crypto.timingSafeEqual (constant-time; guard equal-length before compare). Reject (401) if the header is missing or mismatched. Parse the JSON only after signature passes; verify the pushed ref matches the deploy branch (refs/heads/main) — ignore others (200 no-op). Only on a verified main push, spawn deploy/deploy.sh (detached, log to a file), respond 202. Never log the secret or the raw body. Export the verifySignature(rawBody, header, secret) function for testing. Create deploy/webhook-receiver.test.mjs (node:test + node:assert) asserting: valid signature returns true; tampered body returns false; missing header returns false; wrong-length header returns false (no throw). Create deploy/rentular-webhook.service: a systemd unit that runs `node /opt/rentular/current/deploy/webhook-receiver.mjs` (WorkingDirectory /opt/rentular/current), loads the secret via `EnvironmentFile=/opt/rentular/.env` (for DEPLOY_WEBHOOK_SECRET), Restart=always, runs as a non-root deploy user, and documents the loopback bind (127.0.0.1:9000) in a comment. Mark the unit a reference installed on the host by the operator in Plan 06.
  </action>
  <verify>
    <automated>node --test deploy/webhook-receiver.test.mjs && grep -q "127.0.0.1" deploy/webhook-receiver.mjs && grep -q "webhook-receiver.mjs" deploy/rentular-webhook.service && grep -q "EnvironmentFile=/opt/rentular/.env" deploy/rentular-webhook.service</automated>
  </verify>
  <acceptance_criteria>
    - `node --test deploy/webhook-receiver.test.mjs` passes
    - Uses crypto.timingSafeEqual for signature comparison; listens on 127.0.0.1:9000 (loopback, never 0.0.0.0)
    - Rejects unsigned/invalid/wrong-length signatures with non-2xx; only exec's deploy.sh on a verified deploy-branch push
    - Does not log the secret or raw body
    - deploy/rentular-webhook.service runs webhook-receiver.mjs, sources DEPLOY_WEBHOOK_SECRET via EnvironmentFile=/opt/rentular/.env, Restart=always, non-root user
  </acceptance_criteria>
  <done>Webhook receiver authenticates GitHub pushes via HMAC-SHA256, listens on loopback:9000, and only triggers deploy.sh on a verified main-branch push; a systemd unit runs it as a service.</done>
</task>

<task type="auto">
  <name>Task 3: deploy/smoke.sh + deploy/nginx/rentular.conf</name>
  <files>deploy/smoke.sh, deploy/nginx/rentular.conf</files>
  <read_first>
    - 10-VALIDATION.md § "Manual-Only Verifications" (the exact smoke signals) and § Per-Task map
    - 10-RESEARCH.md § "Pitfall 7" (X-Forwarded-Proto for NextAuth) and the system architecture diagram
    - apps/api/src/index.ts (health at basePath /api/v1 → served /api/v1/health)
    - deploy/webhook-receiver.mjs (Task 2 — the loopback:9000 receiver nginx must reach)
  </read_first>
  <action>
    Create deploy/smoke.sh (`set -euo pipefail`, arg = base URL default https://rentular.com): assert curl HTTPS returns 200; assert `curl -sSf $BASE/api/v1/health` JSON contains `"status":"healthy"`; assert TLS via `openssl s_client -connect <host>:443 -servername <host>` shows valid notAfter dates; assert `curl -s $BASE | grep -c 'localhost:4000'` equals 0 (build-ARG correctness, Pitfall 1); assert `docker compose -f docker-compose.prod.yml ps` shows services healthy/running. Exit non-zero on any failure with a clear message. Create deploy/nginx/rentular.conf: server block for rentular.com — a `location /api/v1 { proxy_pass http://127.0.0.1:4000; }` that PRESERVES the /api/v1 prefix (do NOT strip — the Hono basePath expects it), a `location /deploy-webhook { proxy_pass http://127.0.0.1:9000; }` proxying to the loopback webhook receiver from Task 2 (this is the GitHub webhook target https://rentular.com/deploy-webhook), a `location / { proxy_pass http://127.0.0.1:3000; }`, standard proxy headers including `proxy_set_header X-Forwarded-Proto https;` plus Host/X-Real-IP/X-Forwarded-For, WebSocket upgrade headers, and LE cert paths referencing the existing m1 certbot layout (comment that certs come from existing automation). Mark it a reference file installed on the host by the operator in Plan 06.
  </action>
  <verify>
    <automated>bash -n deploy/smoke.sh && grep -q "api/v1/health" deploy/smoke.sh && grep -q "localhost:4000" deploy/smoke.sh && grep -q "X-Forwarded-Proto" deploy/nginx/rentular.conf && grep -q "/api/v1" deploy/nginx/rentular.conf && grep -q "location /deploy-webhook" deploy/nginx/rentular.conf && grep -q "127.0.0.1:9000" deploy/nginx/rentular.conf</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n deploy/smoke.sh` passes
    - smoke.sh checks HTTPS 200, /api/v1/health healthy, TLS dates, zero localhost:4000 in HTML, compose ps healthy
    - nginx conf preserves the /api/v1 prefix to api:4000, routes / to web:3000, forwards X-Forwarded-Proto https
    - nginx conf contains a `location /deploy-webhook` proxying to the loopback receiver at http://127.0.0.1:9000
  </acceptance_criteria>
  <done>smoke.sh proves the deployment's observable signals; nginx conf splits SSR vs API traffic behind TLS and exposes /deploy-webhook to the loopback receiver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet (GitHub) → webhook receiver | Untrusted POST that triggers arbitrary code execution (deploy) |
| Internet → nginx | TLS termination; only ingress to the app tier |
| deploy.sh → shell | Runs build + docker + bootstrap with box privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Elevation of Privilege | Unauthenticated deploy webhook → RCE | mitigate | HMAC-SHA256 constant-time verify of X-Hub-Signature-256 + deploy-branch allowlist before exec; receiver bound to 127.0.0.1:9000 (only nginx /deploy-webhook reaches it); unit-tested |
| T-10-05-02 | Tampering | Deploy of unverified/forged ref | mitigate | Receiver ignores non-main refs (200 no-op); only verified main push runs deploy.sh |
| T-10-05-03 | Information Disclosure | Deploy logs leaking /opt/rentular/.env | mitigate | deploy.sh SOURCES the secrets file (never cat/echo/print); receiver never logs secret/raw body; systemd unit loads it via EnvironmentFile |
| T-10-05-04 | Spoofing | Insecure cookies / wrong callback behind proxy | mitigate | nginx forwards X-Forwarded-Proto https + Host; pairs with AUTH_URL + trustHost |
| T-10-05-05 | Denial of Service | Broken release flipped live | mitigate | pnpm build gate before symlink flip; failure leaves `current` unchanged; rollback documented |
| T-10-05-SC | Tampering | node --test / no new deps | accept | Receiver uses Node built-in crypto/http only; no new npm packages — no legitimacy gate needed |
</threat_model>

<verification>
- `bash -n deploy/deploy.sh`, `bash -n deploy/smoke.sh` pass
- `node --test deploy/webhook-receiver.test.mjs` passes (signature verify proven)
- deploy.sh gates on pnpm build not lint, sources /opt/rentular/.env, and sets DB_HOST=127.0.0.1 for bootstrap
- nginx preserves /api/v1 prefix and proxies /deploy-webhook to the loopback receiver (127.0.0.1:9000)
</verification>

<success_criteria>
- Atomic build-on-box deploy.sh with correct gate, host-shell bootstrap env sourcing (DB_HOST=127.0.0.1), and rollback semantics
- HMAC-authenticated webhook receiver on loopback:9000, unit-tested, with a systemd unit
- smoke.sh + nginx reference block aligned to VALIDATION signals, exposing /deploy-webhook
</success_criteria>

<output>
Create `.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-05-SUMMARY.md` when done
</output>
</content>
