---
phase: 10-deploy-to-hetzner-m1-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/lib/stripe.ts
  - apps/api/src/routes/stripe.ts
  - apps/api/src/lib/__tests__/stripe.test.ts
  - apps/web/next.config.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "API process boots successfully when STRIPE_SECRET_KEY is unset (no top-level new Stripe() crash)"
    - "Next.js web build emits a standalone server (output: 'standalone')"
    - "API reads all config from process.env only; no dotenv/.env auto-load dependency"
  artifacts:
    - path: "apps/api/src/lib/stripe.ts"
      provides: "isStripeConfigured() + lazy getStripeClient() guard mirroring gocardless.ts"
      contains: "isStripeConfigured"
    - path: "apps/web/next.config.ts"
      provides: "standalone output + outputFileTracingRoot at monorepo root"
      contains: "standalone"
  key_links:
    - from: "apps/api/src/routes/stripe.ts"
      to: "apps/api/src/lib/stripe.ts"
      via: "getStripeClient() called inside handlers, not at module top-level"
      pattern: "getStripeClient"
---

<objective>
Fold in the two prerequisite code fixes (D-09 Stripe boot-guard, D-10 env-loading confirmation) and enable the Next.js standalone build required by the web Dockerfile.

Purpose: The API must not crash at boot when STRIPE_SECRET_KEY is unset (prod may launch subscription-billing-off), and the web app cannot be containerized with the standalone Docker pattern until next.config declares it. These are Wave 1 blockers for every downstream Docker task.
Output: Lazy Stripe client guard + test, standalone next.config, verified process.env-only loading.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.2/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-CONTEXT.md
@.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-RESEARCH.md

<interfaces>
<!-- The guard pattern to mirror. Extracted from apps/api/src/lib/gocardless.ts:47-68 -->
Existing pattern in apps/api/src/lib/gocardless.ts:
  export function getGoCardlessClient(): GoCardlessClient { if (client) return client; const accessToken = process.env.GOCARDLESS_ACCESS_TOKEN; if (!accessToken) throw new Error(...); client = new GoCardless(...); return client; }
  export function isGoCardlessConfigured(): boolean { return !!process.env.GOCARDLESS_ACCESS_TOKEN; }

Current apps/api/src/routes/stripe.ts top-level (line 4) to REMOVE:
  const stripe = new Stripe(process.env.STRIPE_SECRET_KEY || "");
Usages that must switch to a lazily-obtained client: line 44 stripe.prices.retrieve, line 91 stripe.checkout.sessions.create, line 121 stripe.webhooks.constructEvent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lazy Stripe client guard and remove boot-time construction (D-09)</name>
  <files>apps/api/src/lib/stripe.ts, apps/api/src/routes/stripe.ts, apps/api/src/lib/__tests__/stripe.test.ts</files>
  <read_first>
    - apps/api/src/lib/gocardless.ts (the isGoCardlessConfigured / getGoCardlessClient pattern to mirror exactly)
    - apps/api/src/routes/stripe.ts (all three stripe.* usages: lines 44, 91, 121; and the existing !process.env.STRIPE_SECRET_KEY fallback in the /plans handler)
    - apps/api/src/lib/__tests__/ (existing test file layout / vitest conventions)
  </read_first>
  <behavior>
    - Test 1: importing apps/api/src/lib/stripe.ts does NOT throw when STRIPE_SECRET_KEY is unset
    - Test 2: isStripeConfigured() returns false when STRIPE_SECRET_KEY is unset, true when set
    - Test 3: getStripeClient() throws a descriptive Error when STRIPE_SECRET_KEY is unset (lazy, only when called)
  </behavior>
  <action>
    Create apps/api/src/lib/stripe.ts exporting isStripeConfigured() (returns !!process.env.STRIPE_SECRET_KEY) and a memoized getStripeClient() that lazily constructs `new Stripe(process.env.STRIPE_SECRET_KEY)` on first call and throws "STRIPE_SECRET_KEY is not set. Configure it in your environment." when unset — mirror gocardless.ts singleton shape. Refactor apps/api/src/routes/stripe.ts: delete the module-top-level `const stripe = new Stripe(...)` at line 4; call getStripeClient() inside each handler that needs it (prices.retrieve, checkout.sessions.create, webhooks.constructEvent). Preserve the existing static-plans fallback behavior when STRIPE_SECRET_KEY is unset. Use named export `export const stripeRouter`. Add apps/api/src/lib/__tests__/stripe.test.ts covering the three behaviors above (use vi.stubEnv / delete process.env.STRIPE_SECRET_KEY around dynamic import). No Co-Authored-By in any commit.
  </action>
  <verify>
    <automated>pnpm --filter @rentular/api test --run src/lib/__tests__/stripe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "new Stripe(" apps/api/src/routes/stripe.ts` returns NO top-level (line ~4) match; construction happens only inside apps/api/src/lib/stripe.ts
    - `grep -q "isStripeConfigured" apps/api/src/lib/stripe.ts` succeeds
    - `grep -q "getStripeClient" apps/api/src/routes/stripe.ts` succeeds
    - New vitest test file passes; asserts import-without-key does not throw
  </acceptance_criteria>
  <done>API module import is crash-safe with STRIPE_SECRET_KEY unset; Stripe client is lazily constructed; test proves it.</done>
</task>

<task type="auto">
  <name>Task 2: Enable Next.js standalone output and confirm process.env-only loading (D-10)</name>
  <files>apps/web/next.config.ts</files>
  <read_first>
    - apps/web/next.config.ts (current config — only transpilePackages set)
    - apps/api/src/index.ts (confirm all config via process.env; no dotenv import)
    - 10-RESEARCH.md § "Pattern 1" (next.config standalone example with outputFileTracingRoot)
  </read_first>
  <action>
    Edit apps/web/next.config.ts: add `output: "standalone"` and `outputFileTracingRoot: path.join(__dirname, "../../")` (import node:path) to the NextConfig, keeping the existing transpilePackages and the withNextIntl wrapper. Do NOT add any NEXT_PUBLIC_API_URL default here — it is baked as a Docker build ARG in Plan 03. For D-10: this task also VERIFIES (no code change) that the API has no dotenv dependency — the confirmation is the acceptance grep below. If any dotenv import is found in apps/api/src, STOP and report it (research asserts none exists).
  </action>
  <verify>
    <automated>grep -q "output: \"standalone\"" apps/web/next.config.ts && grep -q "outputFileTracingRoot" apps/web/next.config.ts && ! grep -rq "dotenv" apps/api/src</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/next.config.ts contains `output: "standalone"` and `outputFileTracingRoot`
    - `! grep -rq "dotenv" apps/api/src` (D-10 confirmed: no .env auto-load path)
    - withNextIntl wrapper and transpilePackages preserved
  </acceptance_criteria>
  <done>Web build will emit a standalone server; API confirmed to read only process.env (container-injected env is sufficient).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container env → API process | Secrets (STRIPE_SECRET_KEY etc.) arrive via process.env; unset values must not crash boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Denial of Service | API boot with unset STRIPE_SECRET_KEY | mitigate | Lazy getStripeClient() — construction deferred to handler call; unit test proves import is crash-safe |
| T-10-01-02 | Information Disclosure | Stripe error surfaced to client | accept | Error thrown only server-side on handler call; existing /plans fallback returns static data, no secret leak |
| T-10-01-SC | Tampering | npm installs | accept | No new packages added this plan (stripe, path already present) — no legitimacy gate needed |
</threat_model>

<verification>
- `pnpm --filter @rentular/api test --run` (full api suite still green, including new stripe test)
- `pnpm build` green (tsup + next build) — proves standalone config does not break the build
</verification>

<success_criteria>
- API imports without crashing when STRIPE_SECRET_KEY is unset (test-proven)
- next.config.ts declares standalone output + monorepo tracing root
- No dotenv dependency anywhere in apps/api/src
</success_criteria>

<output>
Create `.planning/phases/10-deploy-to-hetzner-m1-production-deployment/10-01-SUMMARY.md` when done
</output>
