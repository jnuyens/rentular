---
phase: 07-ui-polish-onboarding-launch-readiness
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/web/app/(dashboard)/payments/page.tsx
  - apps/web/app/(dashboard)/communications/page.tsx
  - apps/web/app/(dashboard)/indexation/page.tsx
  - apps/web/app/(dashboard)/maintenance/page.tsx
  - apps/web/messages/en/common.json
  - apps/web/messages/nl/common.json
  - apps/web/messages/fr/common.json
  - apps/web/messages/de/common.json
autonomous: true
requirements:
  - UI-05
must_haves:
  truths:
    - "Payments, communications, indexation, and maintenance pages use shadcn/ui components"
    - "All four pages show skeleton loading states while data fetches"
    - "All four pages render as stacked cards on mobile (<768px)"
    - "All four pages use toast for success/error feedback"
    - "All four pages use AlertDialog for destructive confirmations"
  artifacts:
    - path: "apps/web/app/(dashboard)/payments/page.tsx"
      provides: "Payments page with shadcn/ui components"
      contains: "import { Table"
    - path: "apps/web/app/(dashboard)/communications/page.tsx"
      provides: "Communications page with shadcn/ui components"
      contains: "import { Table"
    - path: "apps/web/app/(dashboard)/indexation/page.tsx"
      provides: "Indexation page with shadcn/ui components"
      contains: "import { Table"
    - path: "apps/web/app/(dashboard)/maintenance/page.tsx"
      provides: "Maintenance page with shadcn/ui components"
      contains: "import { Table"
  key_links:
    - from: "apps/web/app/(dashboard)/payments/page.tsx"
      to: "apps/web/components/ui/table.tsx"
      via: "import Table components"
      pattern: "import.*Table.*from"
    - from: "apps/web/app/(dashboard)/maintenance/page.tsx"
      to: "apps/web/components/ui/dialog.tsx"
      via: "import Dialog for modals"
      pattern: "import.*Dialog.*from"
---

<objective>
Migrate the second batch of dashboard pages (payments, communications, indexation, maintenance) to shadcn/ui components with table-to-card mobile responsiveness, loading skeletons, toast notifications, and AlertDialog confirmations.

Purpose: These 4 pages total ~1,949 lines of raw Tailwind that need consistent shadcn/ui treatment to match the first batch (properties, tenants, leases) migrated in Plan 02.

Output: 4 dashboard pages fully migrated to shadcn/ui with responsive mobile layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ui-polish-onboarding-launch-readiness/07-CONTEXT.md
@.planning/phases/07-ui-polish-onboarding-launch-readiness/07-RESEARCH.md
@.planning/phases/07-ui-polish-onboarding-launch-readiness/07-UI-SPEC.md
@.planning/phases/07-ui-polish-onboarding-launch-readiness/07-01-SUMMARY.md

<interfaces>
<!-- shadcn/ui components available from Plan 01: -->
<!-- Table, TableHeader, TableBody, TableHead, TableRow, TableCell from @/components/ui/table -->
<!-- Card, CardContent, CardTitle, CardDescription from @/components/ui/card -->
<!-- Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter from @/components/ui/dialog -->
<!-- AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogAction, AlertDialogCancel, AlertDialogFooter from @/components/ui/alert-dialog -->
<!-- Button from @/components/ui/button -->
<!-- Badge from @/components/ui/badge -->
<!-- Skeleton from @/components/ui/skeleton -->
<!-- Input from @/components/ui/input -->
<!-- Label from @/components/ui/label -->
<!-- Select, SelectTrigger, SelectValue, SelectContent, SelectItem from @/components/ui/select -->
<!-- toast from sonner -->

<!-- Toast and empty state i18n keys already added by Plan 02: -->
<!-- toast.saved, toast.created, toast.deleted, toast.updated, toast.saveFailed, toast.loadFailed, toast.deleteFailed, toast.saving, toast.networkError -->
<!-- dashboard.deleteConfirm, dashboard.delete, dashboard.keepItem -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate payments and communications pages to shadcn/ui with responsive mobile, skeletons, toasts</name>
  <files>
    apps/web/app/(dashboard)/payments/page.tsx
    apps/web/app/(dashboard)/communications/page.tsx
    apps/web/messages/en/common.json
    apps/web/messages/nl/common.json
    apps/web/messages/fr/common.json
    apps/web/messages/de/common.json
  </files>
  <read_first>
    apps/web/app/(dashboard)/payments/page.tsx
    apps/web/app/(dashboard)/communications/page.tsx
    apps/web/messages/en/common.json
  </read_first>
  <action>
    Apply the SAME migration pattern used in Plan 02 Task 2 to both pages. For each page:

    **Payments page (605 lines):**
    - Replace all raw `<button>` -> shadcn `<Button>` with appropriate variants
    - Replace all raw `<input>` -> shadcn `<Input>` with `<Label>`
    - Replace raw `<table>` -> shadcn `<Table>`, `<TableHeader>`, `<TableHead>`, `<TableBody>`, `<TableRow>`, `<TableCell>`
    - Replace custom modal overlays -> shadcn `<Dialog>` for add manual payment dialog
    - Replace `window.confirm()` -> shadcn `<AlertDialog>` for cancel payment confirmation
    - Replace inline HSL `bg-[hsl(var(--primary))]` -> semantic `bg-primary`, `text-muted-foreground`, etc.
    - Payment status badges should use shadcn `<Badge>`:
      - paid: `<Badge variant="default" className="bg-green-100 text-green-700">`
      - overdue: `<Badge variant="destructive">`
      - processing: `<Badge variant="secondary" className="bg-yellow-100 text-yellow-700">`
      - failed: `<Badge variant="destructive">`
      - cancelled: `<Badge variant="outline">`
    - Add table-to-card mobile responsive pattern:
      Desktop: `<div className="hidden md:block"><Table>...</Table></div>`
      Mobile: `<div className="md:hidden space-y-3">{data.map(p => <Card>..primary field + badge + label:value pairs..</Card>)}</div>`
    - Add skeleton loading: Table header + 5 TableRow skeletons while isLoading
    - Add toast: `toast.success(t("toast.created"))` after recording manual payment, `toast.error()` on failure
    - Add empty state when no payments: `dashboard.emptyPaymentsTitle`, `dashboard.emptyPaymentsDesc`

    **Communications page (370 lines):**
    - Replace raw `<table>` -> shadcn `<Table>` components
    - Replace raw buttons -> shadcn `<Button>`
    - Replace inline HSL colors -> semantic classes
    - Communications has expandable rows (click row to see details) -- keep this behavior but with shadcn Table components
    - Status badges use shadcn `<Badge>` (sent, failed, pending)
    - Add table-to-card mobile responsive pattern
    - Add skeleton loading: Table header + 5 TableRow skeletons
    - Add toast: on resend success/failure
    - Add empty state: `dashboard.emptyCommunicationsTitle`, `dashboard.emptyCommunicationsDesc`

    **New i18n keys** to add to all 4 locale files:
    - `dashboard.emptyPaymentsTitle`: EN "No payments yet" / NL "Nog geen betalingen" / FR "Pas encore de paiements" / DE "Noch keine Zahlungen"
    - `dashboard.emptyPaymentsDesc`: EN "Payments will appear here once a lease has been set up." / NL "Betalingen verschijnen hier zodra een huurcontract is ingesteld." / FR "Les paiements apparaitront ici une fois un bail configure." / DE "Zahlungen erscheinen hier, sobald ein Mietvertrag eingerichtet ist."
    - `dashboard.emptyCommunicationsTitle`: EN "No communications yet" / NL "Nog geen communicatie" / FR "Pas encore de communications" / DE "Noch keine Kommunikation"
    - `dashboard.emptyCommunicationsDesc`: EN "Sent emails and SMS messages will be logged here." / NL "Verzonden e-mails en sms-berichten worden hier bijgehouden." / FR "Les e-mails et SMS envoyes seront enregistres ici." / DE "Gesendete E-Mails und SMS-Nachrichten werden hier protokolliert."
  </action>
  <verify>
    <automated>cd /Users/jnuyens/rentular/source && grep -c "import.*Table" "apps/web/app/(dashboard)/payments/page.tsx" && grep -c "import.*Table" "apps/web/app/(dashboard)/communications/page.tsx" && grep "md:hidden" "apps/web/app/(dashboard)/payments/page.tsx" && grep "Skeleton" "apps/web/app/(dashboard)/payments/page.tsx" && grep "toast" "apps/web/app/(dashboard)/payments/page.tsx" && grep "dashboard.emptyPaymentsTitle" apps/web/messages/en/common.json</automated>
  </verify>
  <acceptance_criteria>
    - payments/page.tsx imports Table, Button, Dialog, Card, Skeleton, Badge from @/components/ui/*
    - payments/page.tsx contains `md:hidden` and `hidden md:block` for responsive layout
    - payments/page.tsx imports `toast` from `sonner`
    - payments/page.tsx does NOT contain `window.confirm`
    - payments/page.tsx does NOT contain `bg-[hsl(var(--` inline HSL patterns
    - communications/page.tsx imports Table, Button, Badge, Skeleton from @/components/ui/*
    - communications/page.tsx contains `md:hidden` for mobile cards
    - communications/page.tsx imports `toast` from `sonner`
    - All 4 locale files contain `dashboard.emptyPaymentsTitle` and `dashboard.emptyCommunicationsTitle`
  </acceptance_criteria>
  <done>Payments and communications pages fully migrated to shadcn/ui with responsive mobile cards, skeleton loading, toast notifications, and empty states</done>
</task>

<task type="auto">
  <name>Task 2: Migrate indexation and maintenance pages to shadcn/ui with responsive mobile, skeletons, toasts</name>
  <files>
    apps/web/app/(dashboard)/indexation/page.tsx
    apps/web/app/(dashboard)/maintenance/page.tsx
  </files>
  <read_first>
    apps/web/app/(dashboard)/indexation/page.tsx
    apps/web/app/(dashboard)/maintenance/page.tsx
  </read_first>
  <action>
    Apply the SAME migration pattern to both pages:

    **Indexation page (328 lines):**
    - Replace raw `<table>` -> shadcn `<Table>` components
    - Replace raw buttons -> shadcn `<Button>`
    - Replace raw inputs -> shadcn `<Input>` with `<Label>`
    - Replace custom preview modal -> shadcn `<Dialog>` for indexation preview
    - Replace inline HSL colors -> semantic classes
    - Add shadcn `<Badge>` for indexation status
    - Add table-to-card mobile responsive pattern:
      Desktop: `<div className="hidden md:block"><Table>...</Table></div>`
      Mobile: `<div className="md:hidden space-y-3">{items.map(i => <Card>..label:value pairs..</Card>)}</div>`
    - Add skeleton loading: Table header + 3 TableRow skeletons (fewer rows per UI-SPEC)
    - Add toast: `toast.success()` after applying indexation, `toast.error()` on failure
    - Note: indexation page may not need empty state (it shows calculation interface, not a list)

    **Maintenance page (646 lines):**
    - Replace raw `<table>` -> shadcn `<Table>` components
    - Replace raw buttons -> shadcn `<Button>`
    - Replace raw inputs -> shadcn `<Input>` with `<Label>`
    - Replace custom modals -> shadcn `<Dialog>` for add/edit maintenance task
    - Replace `window.confirm()` -> shadcn `<AlertDialog>` for delete confirmation
    - Replace raw `<select>` -> shadcn `<Select>`, `<SelectTrigger>`, `<SelectValue>`, `<SelectContent>`, `<SelectItem>` for priority dropdown
    - Replace inline HSL colors -> semantic classes
    - Maintenance status badges use shadcn `<Badge>`:
      - completed: green Badge
      - overdue: destructive Badge
      - upcoming: secondary Badge
    - Add table-to-card mobile responsive pattern
    - Add skeleton loading: Table header + 5 TableRow skeletons
    - Add toast: `toast.success()` after create/update/delete, `toast.error()` on failure
    - Use `<AlertDialog>` for delete confirmation with `dashboard.deleteConfirm` / `dashboard.delete` / `dashboard.keepItem` i18n keys (already added by Plan 02)
  </action>
  <verify>
    <automated>cd /Users/jnuyens/rentular/source && grep -c "import.*Table" "apps/web/app/(dashboard)/indexation/page.tsx" && grep -c "import.*Table" "apps/web/app/(dashboard)/maintenance/page.tsx" && grep "md:hidden" "apps/web/app/(dashboard)/maintenance/page.tsx" && grep "Skeleton" "apps/web/app/(dashboard)/maintenance/page.tsx" && grep "Dialog" "apps/web/app/(dashboard)/indexation/page.tsx" && grep "Select" "apps/web/app/(dashboard)/maintenance/page.tsx" && grep "AlertDialog" "apps/web/app/(dashboard)/maintenance/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - indexation/page.tsx imports Table, Button, Dialog, Badge, Skeleton from @/components/ui/*
    - indexation/page.tsx contains `md:hidden` for mobile cards
    - indexation/page.tsx imports `toast` from `sonner`
    - indexation/page.tsx does NOT contain `bg-[hsl(var(--` inline HSL patterns
    - maintenance/page.tsx imports Table, Button, Dialog, Card, Skeleton, Badge, Select, AlertDialog from @/components/ui/*
    - maintenance/page.tsx contains `md:hidden` and `hidden md:block`
    - maintenance/page.tsx imports `toast` from `sonner`
    - maintenance/page.tsx does NOT contain `window.confirm`
    - maintenance/page.tsx does NOT contain `bg-[hsl(var(--` inline HSL patterns
  </acceptance_criteria>
  <done>Indexation and maintenance pages fully migrated to shadcn/ui with responsive mobile cards, skeleton loading, toast notifications, Select dropdowns, and AlertDialog confirmations</done>
</task>

</tasks>

<verification>
1. All 4 pages render correctly with shadcn/ui components on desktop
2. All 4 pages show stacked Card layout on mobile (<768px)
3. Skeleton loading appears while data fetches
4. Toast notifications fire on user actions
5. Maintenance delete uses AlertDialog instead of window.confirm
6. Indexation preview shows in shadcn Dialog
7. No inline HSL color patterns remain in any of the 4 pages
</verification>

<success_criteria>
- All 4 pages (payments, communications, indexation, maintenance) fully migrated to shadcn/ui
- Table-to-card responsive pattern on all 4 pages per D-10
- Skeleton loading on all 4 pages per D-20
- Toast notifications on all 4 pages per D-19
- AlertDialog for destructive actions (no window.confirm)
- Consistent styling across all migrated pages per UI-05
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-polish-onboarding-launch-readiness/07-04-SUMMARY.md`
</output>
