---
phase: 09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - apps/api/src/routes/bankConnections.ts
  - apps/api/src/index.ts
  - apps/api/src/services/bankStatementImporter.ts
  - apps/api/src/services/bankConnectionSync.ts
  - apps/api/src/jobs/paymentCheckWorker.ts
  - apps/api/src/services/__tests__/bankStatementImporter.test.ts
  - apps/api/src/routes/__tests__/bankConnections.test.ts
autonomous: true
requirements:
  - BANK-ROUTES
  - BANK-WORKER
  - BANK-MATCHER
user_setup: []
tags:
  - hono
  - api
  - oauth
  - bullmq
  - drizzle
  - psd2

must_haves:
  truths:
    - "POST /api/v1/bank-connections returns a Ponto authorization URL with a signed state JWT"
    - "GET /api/v1/bank-connections/callback verifies state JWT, exchanges code for tokens, encrypts tokens, persists bank_connections row with status active and provider-sourced consentExpiresAt"
    - "GET /api/v1/bank-connections lists current owner's connections with token columns stripped"
    - "GET /api/v1/bank-connections/:id returns a single owner-scoped connection"
    - "POST /api/v1/bank-connections/:id/renew initiates re-consent and returns a fresh authorization URL"
    - "DELETE /api/v1/bank-connections/:id calls revokeAccess then soft-deletes (status revoked); bank_statements retained"
    - "POST /api/v1/bank-connections/:id/sync enqueues an immediate sync job rate-limited to 1/min per connection"
    - "GET /api/v1/bank-connections/institutions?country=BE proxies the Ponto institutions list"
    - "bankStatementImporter encrypts counterpartyName/IBAN/rawPayload at rest and dedups via UNIQUE constraint"
    - "syncBankConnection service is shared by the route and the worker (single source of truth)"
    - "paymentCheckWorker Phase B calls syncBankConnection instead of inline matcher; first-sync backfill window is 90 days"
    - "All POST/DELETE routes covered by existing CSRF middleware (verified by routes appearing under protectedPrefixes)"
  artifacts:
    - path: "apps/api/src/routes/bankConnections.ts"
      provides: "Hono router with 8 endpoints (POST /, GET /, GET /callback, GET /:id, GET /institutions, POST /:id/renew, DELETE /:id, POST /:id/sync)"
      contains: "export const bankConnectionsRouter"
    - path: "apps/api/src/index.ts"
      provides: "bankConnectionsRouter mounted at /bank-connections and added to protectedPrefixes"
      contains: "app.route(\"/bank-connections\", bankConnectionsRouter)"
    - path: "apps/api/src/services/bankStatementImporter.ts"
      provides: "Encrypted dedup-safe importer that inserts IncomingTransaction[] into bank_statements"
      contains: "export async function importBankStatements"
    - path: "apps/api/src/services/bankConnectionSync.ts"
      provides: "syncBankConnection service called by worker and POST /:id/sync"
      contains: "export async function syncBankConnection"
    - path: "apps/api/src/jobs/paymentCheckWorker.ts"
      provides: "Phase B refactored to call syncBankConnection; 90-day first-sync backfill"
      contains: "syncBankConnection"
  key_links:
    - from: "apps/api/src/routes/bankConnections.ts"
      to: "apps/api/src/services/bankConnectionSync.ts"
      via: "POST /:id/sync calls syncBankConnection"
      pattern: "syncBankConnection\\("
    - from: "apps/api/src/jobs/paymentCheckWorker.ts"
      to: "apps/api/src/services/bankConnectionSync.ts"
      via: "Phase B iterates active connections and calls syncBankConnection"
      pattern: "syncBankConnection\\(conn\\.id"
    - from: "apps/api/src/services/bankConnectionSync.ts"
      to: "apps/api/src/services/bankStatementImporter.ts"
      via: "Imports rows via importBankStatements BEFORE matcher"
      pattern: "importBankStatements\\("
    - from: "apps/api/src/services/bankStatementImporter.ts"
      to: "apps/api/src/lib/encryption.ts"
      via: "encrypt counterparty name, IBAN, rawPayload"
      pattern: "encrypt\\("
    - from: "apps/api/src/routes/bankConnections.ts"
      to: "apps/api/src/lib/bankOAuthState.ts"
      via: "sign and verify OAuth state JWT"
      pattern: "signOAuthState\\(|verifyOAuthState\\("
---

<objective>
Wire the API surface that the dashboard UI (Plan 04) consumes: 8 Hono routes for the connection lifecycle, the encrypted statement importer, the sync service shared with the polling worker, and a worker refactor that eliminates duplication between cron-driven and manual sync paths.

Purpose: Plan 02 left provider mechanics and OAuth state helpers as building blocks. Plan 03 stitches them into Rentular's HTTP API and BullMQ worker so the end-to-end Customer-Paying flow works: landlord clicks Connect, lands on Ponto, returns, tokens are encrypted at rest, polling worker picks up the new active connection on its next cycle and starts populating bank_statements (audit trail) which the existing Phase 2 matcher reads.

Output: One new Hono router file (8 endpoints), one bootstrap modification, two new service files, one worker modification, and two new test files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-CONTEXT.md
@.planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-RESEARCH.md
@.planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-PATTERNS.md
@.planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-01-PLAN.md
@.planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-02-PLAN.md
@apps/api/src/routes/gocardless.ts
@apps/api/src/routes/bankAccounts.ts
@apps/api/src/lib/routeAuth.ts
@apps/api/src/lib/bankAccountData.ts
@apps/api/src/lib/bankOAuthState.ts
@apps/api/src/lib/pontoConnect.ts
@apps/api/src/lib/encryption.ts
@apps/api/src/services/transactionMatcher.ts
@apps/api/src/jobs/paymentCheckWorker.ts
@apps/api/src/index.ts
@apps/api/src/routes/__tests__/settings.test.ts

<interfaces>
Existing route auth helper (apps/api/src/lib/routeAuth.ts):

```typescript
export function getRequiredUserId(c: Context): string;  // throws if userId not on context
```

Existing matcher signature (apps/api/src/services/transactionMatcher.ts):

```typescript
export async function processIncomingTransactions(
  ownerId: string,
  transactions: IncomingTransaction[]
): Promise<{ matched: number; mismatched: number; unmatched: number }>;
```

Existing encryption helper:

```typescript
export function encrypt(plaintext: string): { encrypted: string; iv: string; tag: string };
export function decrypt(encrypted: string, iv: string, tag: string): string;
```

Existing PontoConnectProvider (Plan 02) signature:

```typescript
new PontoConnectProvider({ accessToken, refreshToken }).setTokens(...).getTransactions({ accountId, dateFrom });
```

Existing Hono mount + CSRF protection in apps/api/src/index.ts:43-91 — adding /bank-connections to protectedPrefixes automatically enables auth + CSRF on the route group.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build bankStatementImporter + bankConnectionSync services (encrypted, dedup-safe, shared by worker and route)</name>
  <read_first>
    - apps/api/src/services/transactionMatcher.ts (full file — service pattern, normalization, return shape)
    - apps/api/src/lib/encryption.ts (full file)
    - apps/api/src/lib/bankAccountData.ts (full file after Plan 02 — PontoConnectProvider class shape)
    - apps/api/src/jobs/paymentCheckWorker.ts (lines 240-318 — Phase B current inline implementation)
    - .planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-PATTERNS.md (Sections 10 and 11 lines 491-577 — service shapes)
    - .planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-RESEARCH.md (Pattern 3 lines 241-279 — dedup-on-import)
  </read_first>
  <behavior>
    - importBankStatements receives connectionId + IncomingTransaction[], encrypts counterpartyName/IBAN/rawPayload, normalizes structured comm to digits-only, attempts INSERT, swallows ER_DUP_ENTRY (1062) errors, returns only newly-inserted rows
    - syncBankConnection loads bank_connections row, refuses if not active (returns zeroed result), decrypts accessToken, constructs PontoConnectProvider with tokens, fetches transactions, calls importBankStatements, calls processIncomingTransactions (matcher), persists matcher results into bank_statements.matchedPaymentId + matchStatus + matchedAt, updates lastSyncAt
    - importBankStatements with the same externalTransactionId twice inserts only once (dedup test)
    - Encryption test: stored counterpartyNameEncrypted is NOT equal to the plaintext
  </behavior>
  <files>
    apps/api/src/services/bankStatementImporter.ts,
    apps/api/src/services/bankConnectionSync.ts,
    apps/api/src/services/__tests__/bankStatementImporter.test.ts
  </files>
  <action>
    1. CREATE apps/api/src/services/bankStatementImporter.ts.

    Function signature:
    ```typescript
    export interface ImportedStatement {
      id: string;                       // newly assigned UUID
      externalTransactionId: string;
      amount: number;
      bookingDate: string;
      structuredCommunicationDigits: string | null;
      debtorName: string | null;
      debtorIban: string | null;
    }

    export async function importBankStatements(
      connectionId: string,
      transactions: IncomingTransaction[]
    ): Promise<{ inserted: ImportedStatement[]; skippedDuplicates: number }>
    ```

    Implementation:
    - Import getDb, bankStatements from "@rentular/db"
    - Import encrypt from "../lib/encryption"
    - Import IncomingTransaction type from "../lib/bankAccountData"
    - Import randomUUID from "crypto"
    - For each transaction:
      - Generate UUID
      - encrypt counterparty name (use empty string when undefined → still encrypt, store cipher), produce 3 columns
      - encrypt counterparty IBAN (same pattern)
      - encrypt JSON.stringify(transaction) for rawPayload (always required — bank_statements.rawPayloadEncrypted is notNull per Plan 01)
      - normalize structuredCommunicationDigits = transaction.remittanceStructured ? transaction.remittanceStructured.replace(/[^0-9]/g, "") : null
      - Attempt INSERT into bankStatements with all encrypted triplets + plain numeric/date columns + matchStatus="unmatched"
      - On error: inspect err.code and err.message; if it contains "ER_DUP_ENTRY" or "Duplicate entry" treat as expected dedup; increment skippedDuplicates; do NOT throw
      - Else rethrow
    - Use try/catch per row, NOT batch INSERT (a single duplicate would abort the batch). Per-row try/catch matches existing transactionMatcher.ts inner-loop pattern.
    - Return inserted rows (ImportedStatement shape; only plaintext-safe fields for downstream callers)
    - Log prefix [BankStatementImporter]

    2. CREATE apps/api/src/services/bankConnectionSync.ts.

    Function signature:
    ```typescript
    export async function syncBankConnection(connectionId: string): Promise<{
      fetched: number; matched: number; mismatched: number; unmatched: number; skippedDuplicates: number;
    }>
    ```

    Implementation:
    - Import getDb, bankConnections, bankStatements from "@rentular/db"
    - Import { eq, and } from "drizzle-orm"
    - Import getBankAccountDataProvider from "../lib/bankAccountData"
    - Import { decrypt } from "../lib/encryption"
    - Import { importBankStatements } from "./bankStatementImporter"
    - Import { processIncomingTransactions } from "./transactionMatcher"
    - Load the connection by id (single row). If not found, throw "Connection {id} not found".
    - If status !== "active", log skip and return zeros.
    - Decrypt accessToken using row.encryptedAccessToken + tokenIv + tokenAuthTag; same for refreshToken.
    - Construct provider via getBankAccountDataProvider({ accessToken, refreshToken }). (Factory in Plan 02 accepts optional tokens.)
    - Compute dateFrom: if conn.lastSyncAt present, use lastSyncAt.toISOString().split("T")[0]; else use 90 days ago (CHANGED from existing 3 days — first-sync backfill per RESEARCH Pitfall 8).
    - Fetch transactions via provider.getTransactions({ accountId: conn.externalAccountId, dateFrom }).
    - Call importBankStatements(connectionId, transactions) — returns inserted rows + skippedDuplicates count.
    - Call processIncomingTransactions(conn.ownerId, inserted-row-shaped-IncomingTransactions). To preserve type fidelity, map inserted rows back into IncomingTransaction shape for the matcher.
    - For each matcher result that produced a matched payment, UPDATE bank_statements SET matchedPaymentId, matchStatus="matched" (or "mismatched_amount" or "unmatched"), matchedAt=now() WHERE externalTransactionId IN (...) AND connectionId=...
    - Update bank_connections SET lastSyncAt=now(), errorMessage=null, updatedAt=now() WHERE id=connectionId.
    - On any thrown error, do NOT swallow — caller (worker or route) decides. Worker's per-iteration try/catch handles failure logging into errorMessage column.
    - Log prefix [BankSync]

    3. CREATE apps/api/src/services/__tests__/bankStatementImporter.test.ts.

    Test structure (vitest with vi.mock for DB):
    - vi.mock("@rentular/db", ...) returning a mock that supports db.insert(...).values(...) chain; uses a closure-captured array for "inserted rows"
    - Set process.env.AUTH_SECRET to a test secret so encrypt works deterministically
    - Test 1 "encrypts counterpartyName at rest": call importBankStatements with one transaction having debtorName="Jan Janssens"; capture the values passed to db.insert; assert counterpartyNameEncrypted !== "Jan Janssens" AND counterpartyNameIv is a non-empty string AND counterpartyNameAuthTag is non-empty
    - Test 2 "dedups on duplicate externalTransactionId": configure mock to throw on second insert with code "ER_DUP_ENTRY"; call importBankStatements with two transactions sharing transactionId; assert inserted.length === 1 and skippedDuplicates === 1
    - Test 3 "normalizes structured communication to digits-only": call with remittanceStructured="+++001/2345/67890+++"; assert the stored structuredCommunication value equals "0012345678906" (note: 12 digits + ISO 11649 check digit, depending on input — adjust to exact regex strip output)

    Conventions: db access via getDb singleton, log prefix [BankStatementImporter] / [BankSync], encryption ALWAYS applied to PII columns, no token leakage.
  </action>
  <verify>
    <automated>cd /Users/jnuyens/rentular/source &amp;&amp; pnpm --filter @rentular/api test -- src/services/__tests__/bankStatementImporter.test.ts --run &amp;&amp; pnpm --filter @rentular/api lint</automated>
  </verify>
  <acceptance_criteria>
    - File apps/api/src/services/bankStatementImporter.ts exists
    - File apps/api/src/services/bankConnectionSync.ts exists
    - grep on bankStatementImporter.ts contains "encrypt(" at least 3 times (name, IBAN, payload)
    - grep on bankConnectionSync.ts contains "importBankStatements(" exactly once
    - grep on bankConnectionSync.ts contains "processIncomingTransactions(" exactly once
    - grep on bankConnectionSync.ts contains "90 * 24 * 60 * 60" (90-day backfill) OR equivalent date arithmetic
    - All 3 bankStatementImporter test cases pass
    - pnpm --filter @rentular/api lint passes
  </acceptance_criteria>
  <done>Both services exist, encrypted importer dedups on UNIQUE constraint, syncBankConnection orchestrates the full pipeline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create bankConnections Hono router with 8 endpoints, mount in index.ts, add to protectedPrefixes</name>
  <read_first>
    - apps/api/src/routes/gocardless.ts (full file — Hono router pattern, zValidator, ownership check, error handling, 503 gate)
    - apps/api/src/routes/bankAccounts.ts (full file — Hono router with auth-only ownership check, ibanSchema validator)
    - apps/api/src/index.ts (full file — mount + protectedPrefixes pattern)
    - apps/api/src/lib/routeAuth.ts (full file — getRequiredUserId signature)
    - apps/api/src/lib/bankOAuthState.ts (just created in Plan 02)
    - apps/api/src/lib/pontoConnect.ts (just created in Plan 02)
    - apps/api/src/lib/encryption.ts (full file — encrypt/decrypt for token storage)
    - .planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-PATTERNS.md (Sections 5 and 6 lines 180-337 — route pattern; Sections "Shared Patterns" lines 1034-1167)
    - .planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-CONTEXT.md (lines 62-72 — endpoint list; lines 78-86 — security; lines 88-91 — renewal flow)
  </read_first>
  <files>apps/api/src/routes/bankConnections.ts, apps/api/src/index.ts</files>
  <action>
    1. CREATE apps/api/src/routes/bankConnections.ts.

    Imports:
    ```typescript
    import { Hono } from "hono";
    import { zValidator } from "@hono/zod-validator";
    import { z } from "zod";
    import { eq, and, desc } from "drizzle-orm";
    import { randomUUID } from "crypto";
    import { getDb, bankConnections } from "@rentular/db";
    import { getRequiredUserId } from "../lib/routeAuth";
    import { signOAuthState, verifyOAuthState } from "../lib/bankOAuthState";
    import {
      isPontoConfigured,
      createPontoAuthorizationUrl,
      exchangeAuthorizationCode,
      revokeAccess,
      listAccounts,
      listFinancialInstitutions,
      getRedirectUri,
    } from "../lib/pontoConnect";
    import { encrypt, decrypt } from "../lib/encryption";
    import { syncBankConnection } from "../services/bankConnectionSync";

    export const bankConnectionsRouter = new Hono();
    ```

    Endpoints (in this exact order; Hono matches static routes first, so /institutions and /callback MUST come before /:id):

    a. GET /institutions
       - zValidator("query", z.object({ country: z.string().length(2).default("BE") }))
       - getRequiredUserId(c) (auth — owner must be authenticated to fetch picker data)
       - if (!isPontoConfigured()) return c.json({ error: "Bank data provider not configured" }, 503)
       - const institutions = await listFinancialInstitutions(country)
       - return c.json({ data: institutions })

    b. POST /
       - zValidator("json", z.object({ institutionId: z.string().min(1) }))
       - getRequiredUserId(c)
       - if (!isPontoConfigured()) → 503
       - const state = await signOAuthState({ ownerId: userId, institutionId: data.institutionId })
       - const consentLink = createPontoAuthorizationUrl({ state })
       - INSERT bank_connections row with id=randomUUID, ownerId, provider="ponto", institutionId, status="pending", country="BE"
       - return c.json({ data: { id, consentLink } }, 201)

    c. GET /callback
       - zValidator("query", z.object({ code: z.string().optional(), state: z.string().optional(), error: z.string().optional(), error_description: z.string().optional() }))
       - NOTE: no requireAuth on the inner handler — this route is reached by the user agent following a redirect from Ponto; the state JWT IS the authentication.
       - if (query.error) return c.redirect(`${process.env.WEB_URL}/dashboard/bank-connections/callback?error=${encodeURIComponent(query.error)}`)
       - if (!query.code || !query.state) return c.redirect(`${WEB_URL}/dashboard/bank-connections/callback?error=missing_params`)
       - try { const payload = await verifyOAuthState(query.state); } catch { redirect to ?error=expired_state }
       - const tokens = await exchangeAuthorizationCode(query.code)
       - const accounts = await listAccounts({ accessToken: tokens.accessToken })
       - if (accounts.length === 0) redirect to ?error=no_accounts
       - Take accounts[0] (v1 takes first account per CONTEXT line 42).
       - const encAccess = encrypt(tokens.accessToken); const encRefresh = encrypt(tokens.refreshToken)
       - Compute consentExpiresAt: parse accounts[0].authorizationExpirationExpectedAt if present, else now + 180 days (CONTEXT line 56-58: source from provider response, NOT hardcoded; the 180-day default is the EBA upper bound used only if provider doesn't return one)
       - If payload.connectionId present (renewal flow): UPDATE existing bank_connections row SET encryptedAccessToken+IV+AuthTag, encryptedRefreshToken+IV+AuthTag, externalAccountId=accounts[0].accountId, iban=accounts[0].iban, status="active", consentExpiresAt, providerMetadata={ accountIds:[accountId] }, errorMessage=null, updatedAt=now() WHERE id=connectionId AND ownerId=payload.ownerId
       - Else (new connection): UPDATE the pending bank_connections row that this owner created (find by ownerId+status="pending"+institutionId=payload.institutionId, most recent createdAt) SET the same fields plus status="active"
       - return c.redirect(`${WEB_URL}/dashboard/bank-connections/${connectionId}?connected=1`)

    d. GET /
       - getRequiredUserId(c)
       - SELECT from bankConnections WHERE ownerId=userId ORDER BY createdAt DESC
       - Strip token columns from response (return { id, provider, institutionId, institutionName, iban, status, consentExpiresAt, lastSyncAt, errorMessage, country, createdAt, updatedAt } only — NO encryptedAccessToken, NO providerMetadata)
       - return c.json({ data: sanitized })

    e. GET /:id
       - getRequiredUserId(c)
       - SELECT WHERE id=c.req.param("id") AND ownerId=userId LIMIT 1
       - If not found → c.json({ error: "Connection not found" }, 404)
       - Strip token columns; return c.json({ data: sanitized })

    f. POST /:id/renew
       - getRequiredUserId(c); load connection by id+ownerId; 404 if missing
       - if (!isPontoConfigured()) → 503
       - const state = await signOAuthState({ ownerId: userId, connectionId: id, institutionId: conn.institutionId })
       - const consentLink = createPontoAuthorizationUrl({ state })
       - return c.json({ data: { consentLink } })

    g. DELETE /:id
       - getRequiredUserId(c); load connection by id+ownerId; 404 if missing
       - if (conn.encryptedAccessToken) try { decrypt and revokeAccess(token) } catch (logs but continues — revoke is best-effort)
       - UPDATE bank_connections SET status="revoked", encryptedAccessToken=null, tokenIv=null, tokenAuthTag=null, encryptedRefreshToken=null, refreshTokenIv=null, refreshTokenAuthTag=null, updatedAt=now() WHERE id=...
       - Do NOT delete bank_statements rows (retention)
       - return c.json({ data: { id, status: "revoked" } })

    h. POST /:id/sync
       - getRequiredUserId(c); load connection by id+ownerId; 404 if missing
       - Rate-limit: simple in-memory Map of `${ownerId}:${id}` → last-call timestamp; reject 429 if < 60s elapsed (CONTEXT line 156 — Redis-based rate limiter optional for v1)
       - try { const result = await syncBankConnection(id) } catch err → c.json({ error }, 500)
       - return c.json({ data: result })

    Error handling: wrap each handler body in try/catch; log "[BankConnections] <operation> error:" prefix; return c.json({ error: message }, 500).

    2. MODIFY apps/api/src/index.ts:

    a. Add import after the bankAccountsRouter import:
       ```typescript
       import { bankConnectionsRouter } from "./routes/bankConnections";
       ```

    b. Add "/bank-connections" to the protectedPrefixes array between "/bank-accounts" and "/property-managers".
       NOTE: This makes auth + CSRF apply automatically to /api/v1/bank-connections/*. But GET /callback is reached after a Ponto redirect and has no Cookie set when the browser follows the redirect — it MUST work without auth. The existing CSRF middleware (apps/api/src/index.ts:74-82) only blocks state-changing methods (POST/DELETE/etc.), not GET. The auth middleware (line 83 + line 84-87) requires userId for protectedPrefixes — this WOULD block /callback. Solution: add a path-prefix exemption inside the requireAuth wrapper similar to the webhook exclusion. SPECIFICALLY:

       Replace the requireAuth loop block (lines 84-87):
       ```typescript
       for (const prefix of protectedPrefixes) {
         app.use(prefix, requireAuth);
         app.use(`${prefix}/*`, requireAuth);
       }
       ```
       With:
       ```typescript
       for (const prefix of protectedPrefixes) {
         app.use(prefix, requireAuth);
         app.use(`${prefix}/*`, async (c, next) => {
           // /bank-connections/callback uses state JWT instead of session auth
           if (c.req.path.endsWith("/bank-connections/callback")) return next();
           return requireAuth(c, next);
         });
       }
       ```
       This preserves auth on every existing route and adds the single callback exemption. (Alternative: add bank-connections to a callback-exempt list and use existing requireAuth for the rest — pick whichever feels more readable but verify all other routes still require auth.)

    c. Add the route mount near the existing app.route lines (between "/bank-accounts" and "/gocardless"):
       ```typescript
       app.route("/bank-connections", bankConnectionsRouter);
       ```

    Conventions: log prefix [BankConnections], response envelope { data } / { error, status }, c.req.param("id") for path params, zValidator for body/query, c.redirect for callback success/error UX.
  </action>
  <verify>
    <automated>cd /Users/jnuyens/rentular/source &amp;&amp; grep -c "bankConnectionsRouter" apps/api/src/index.ts | awk '{ exit ($1 &gt;= 2 ? 0 : 1) }' &amp;&amp; grep -q '"/bank-connections"' apps/api/src/index.ts &amp;&amp; grep -q 'app.route("/bank-connections"' apps/api/src/index.ts &amp;&amp; grep -c 'bankConnectionsRouter.get\|bankConnectionsRouter.post\|bankConnectionsRouter.delete' apps/api/src/routes/bankConnections.ts | awk '{ exit ($1 &gt;= 8 ? 0 : 1) }' &amp;&amp; pnpm --filter @rentular/api lint</automated>
  </verify>
  <acceptance_criteria>
    - File apps/api/src/routes/bankConnections.ts exists
    - grep on apps/api/src/routes/bankConnections.ts for "export const bankConnectionsRouter" returns 1
    - Combined count of bankConnectionsRouter.get / bankConnectionsRouter.post / bankConnectionsRouter.delete is at least 8 (GET /, GET /:id, GET /institutions, GET /callback, POST /, POST /:id/renew, DELETE /:id, POST /:id/sync)
    - grep on apps/api/src/routes/bankConnections.ts for "verifyOAuthState" returns at least 1
    - grep on apps/api/src/routes/bankConnections.ts for "encrypt(" returns at least 2 (access + refresh token encryption)
    - grep on apps/api/src/routes/bankConnections.ts for "consentExpiresAt" returns at least 2 (write paths)
    - grep on apps/api/src/index.ts for "bankConnectionsRouter" returns at least 2 (import + mount)
    - grep on apps/api/src/index.ts for "/bank-connections/callback" returns at least 1 (auth exemption)
    - pnpm --filter @rentular/api lint passes
  </acceptance_criteria>
  <done>Hono router with 8 endpoints exists; mounted at /bank-connections; auth + CSRF apply except to /callback which uses state JWT; tokens encrypted at rest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor paymentCheckWorker Phase B to call syncBankConnection + route integration test</name>
  <read_first>
    - apps/api/src/jobs/paymentCheckWorker.ts (lines 240-318 — Phase B current implementation; lines 320-398 — Phase C unchanged)
    - apps/api/src/services/bankConnectionSync.ts (just created in Task 1)
    - apps/api/src/routes/bankConnections.ts (just created in Task 2)
    - apps/api/src/routes/__tests__/settings.test.ts (vitest mock pattern for routes)
    - .planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-PATTERNS.md (Section 12 lines 578-625 — worker refactor)
  </read_first>
  <behavior>
    - Phase B no longer contains inline matcher calls or inline transaction-fetching; it delegates to syncBankConnection
    - First-sync window is 90 days (delegated to syncBankConnection — worker code change: removes the inline 3-day fallback)
    - Per-connection errors logged into bank_connections.errorMessage column on caught exceptions (preserved behavior)
    - Route test: POST /bank-connections/institutions/ requires auth (401 without session), 503 when env not configured
    - Route test: POST / persists a pending bank_connections row with signed state
    - Route test: GET /callback with tampered state returns redirect to error param
  </behavior>
  <files>apps/api/src/jobs/paymentCheckWorker.ts, apps/api/src/routes/__tests__/bankConnections.test.ts</files>
  <action>
    1. MODIFY apps/api/src/jobs/paymentCheckWorker.ts Phase B (lines 248-318):

    Replace the inner for-of body (lines 264-310) with:
    ```typescript
    for (const conn of activeConnections) {
      try {
        const result = await syncBankConnection(conn.id);
        totalMatched += result.matched;
        totalMismatched += result.mismatched;
        console.log(
          `[PaymentCheck] Bank ${conn.iban || conn.id}: fetched=${result.fetched} matched=${result.matched} mismatched=${result.mismatched} unmatched=${result.unmatched} skippedDuplicates=${result.skippedDuplicates}`
        );
      } catch (err) {
        console.error(`[PaymentCheck] Failed to poll bank connection ${conn.id}:`, err);
        await db
          .update(bankConnections)
          .set({ errorMessage: String(err), updatedAt: new Date() })
          .where(eq(bankConnections.id, conn.id));
      }
    }
    ```

    Add import at top of file (after the existing imports):
    ```typescript
    import { syncBankConnection } from "../services/bankConnectionSync";
    ```

    Remove the now-unused dateFrom calculation block and the inline provider/getTransactions/processIncomingTransactions imports IF no other code in this file depends on them. (Phase B was the only inline caller.) Keep `getBankAccountDataProvider` import only if Phase C uses it for renewConsent — yes, Phase C does (line 352), so keep it.

    Phase C (lines 320-398) requires NO changes. The 7-day/1-day consent expiry warning already correctly reads consentExpiresAt and queueEmail.

    2. CREATE apps/api/src/routes/__tests__/bankConnections.test.ts.

    Test structure (mirroring settings.test.ts vi.mock pattern):
    - vi.mock("@rentular/db", ...) returning getDb/bankConnections/eq/and/desc mocks
    - vi.mock("bullmq", { Queue: MockQueue, Worker: MockWorker }) (matches settings.test.ts:14-19 pattern)
    - vi.mock("../../lib/pontoConnect", ...) returning isPontoConfigured/listFinancialInstitutions/createPontoAuthorizationUrl/exchangeAuthorizationCode/listAccounts/revokeAccess/getRedirectUri mocks
    - vi.mock("../../lib/encryption", ...) returning encrypt/decrypt mocks (encrypt returns deterministic { encrypted: "ENC-"+input, iv: "IV", tag: "TAG" })
    - vi.mock("../../services/bankConnectionSync", ...) returning syncBankConnection mock

    Three test cases:

    a. "BANK-ROUTES: GET /institutions returns 503 when Ponto not configured"
       - Configure isPontoConfigured mock to return false
       - Build a Hono app, mount router, simulate authenticated request via header injection
       - Expect 503 with { error: "Bank data provider not configured" }

    b. "BANK-ROUTES: POST / inserts a pending connection and returns consentLink"
       - Configure isPontoConfigured → true
       - Configure createPontoAuthorizationUrl → returns "https://authorization.myponto.com/oauth2/authorize?client_id=x&state=STATE"
       - Stub the auth middleware to set userId = "owner-test"
       - POST /bank-connections with body { institutionId: "fixture-belfius" }
       - Expect 201 and response body.data.consentLink starts with "https://authorization.myponto.com"
       - Assert db.insert was called with bankConnections values containing ownerId="owner-test", institutionId="fixture-belfius", status="pending"

    c. "BANK-OAUTH: GET /callback with tampered state redirects to ?error=expired_state"
       - Stub verifyOAuthState to throw new Error("Bad signature")
       - Call GET /bank-connections/callback?code=test&state=BAD
       - Expect 302 redirect with location containing "error=expired_state"

    Conventions: vi.mock all DB/queue/provider boundaries; dynamic await import("../bankConnections") for fresh module state per test; assertions on (mockFn).toHaveBeenCalledWith for the DB insert payload.
  </action>
  <verify>
    <automated>cd /Users/jnuyens/rentular/source &amp;&amp; grep -q "syncBankConnection" apps/api/src/jobs/paymentCheckWorker.ts &amp;&amp; ! grep -q "3 \* 24 \* 60 \* 60 \* 1000" apps/api/src/jobs/paymentCheckWorker.ts &amp;&amp; pnpm --filter @rentular/api test -- src/routes/__tests__/bankConnections.test.ts --run &amp;&amp; pnpm --filter @rentular/api lint</automated>
  </verify>
  <acceptance_criteria>
    - grep on apps/api/src/jobs/paymentCheckWorker.ts for "syncBankConnection" returns at least 1
    - grep on apps/api/src/jobs/paymentCheckWorker.ts for the literal "3 * 24 * 60 * 60 * 1000" returns 0 (inline 3-day window removed)
    - grep on apps/api/src/jobs/paymentCheckWorker.ts for "Phase C" still present (no accidental removal)
    - grep on apps/api/src/routes/__tests__/bankConnections.test.ts for "BANK-ROUTES\|BANK-OAUTH" returns at least 2
    - All 3 route test cases pass
    - pnpm --filter @rentular/api lint passes
    - pnpm --filter @rentular/api build succeeds (tsup bundle)
  </acceptance_criteria>
  <done>Worker Phase B delegates to syncBankConnection; first-sync window is 90 days; route tests cover 503/insert/callback-state-tamper paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ POST /bank-connections | Cookie-authenticated; CSRF middleware blocks cross-origin POSTs |
| browser → GET /bank-connections/callback | Reached via redirect from Ponto; state JWT replaces session auth |
| API server ↔ Ponto REST | HTTPS outbound; bearer-token auth using decrypted per-landlord accessToken |
| BullMQ worker ↔ syncBankConnection | In-process; runs as same Node process; no auth surface |
| API server ↔ MySQL | Encrypted tokens at rest; PII (counterpartyName/IBAN) encrypted in bank_statements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Spoofing | GET /callback (no session) | mitigate | verifyOAuthState rejects tampered, wrong-secret, or expired state JWTs; test case 3 covers this; verifier throws and route redirects to ?error=expired_state |
| T-09-03-02 | Tampering | bank_connections row replacement via crafted state | mitigate | state JWT embeds ownerId; UPDATE WHERE ownerId=payload.ownerId scopes mutation to verified principal |
| T-09-03-03 | Information Disclosure | encryptedAccessToken returned by GET /:id or GET / | mitigate | Sanitization step explicitly strips token columns before c.json; acceptance criterion enforces stripping in route code; encryption is defense-in-depth |
| T-09-03-04 | Information Disclosure | counterpartyName/IBAN in bank_statements | mitigate | bankStatementImporter encrypts via lib/encryption.ts; only ciphertext + IV + tag columns exist in schema; no plaintext column |
| T-09-03-05 | Denial of Service | POST /:id/sync flooded by single user | mitigate | In-memory rate limiter 1/min per connection (CONTEXT line 156); 429 response on burst |
| T-09-03-06 | Repudiation | Connection create/revoke without audit | accept | bank_connections.createdAt + updatedAt + status transitions provide audit; full audit log table deferred |
| T-09-03-07 | Tampering | replay of valid state JWT within 10-min window | mitigate | Nonce per-token (UUID); v1 relies on TTL only; Plan 04 UI never re-opens an authorized URL; nonce tracking deferred per CONTEXT decisions (planner discretion) |
| T-09-03-08 | Elevation of Privilege | cross-tenant connection access via crafted :id | mitigate | All :id routes enforce WHERE id AND ownerId=userId; 404 on missing or wrong-owner |
| T-09-03-09 | Information Disclosure | accessToken leaked in worker error logs | mitigate | bankConnectionSync only logs row id; pontoConnect.ts logs no tokens (verified in Plan 02 acceptance criteria); errorMessage column stores stringified err which MUST NOT include the token — verified in test case for syncBankConnection error path |
</threat_model>

<verification>
- 8 endpoints registered in bankConnectionsRouter and mounted at /api/v1/bank-connections
- /bank-connections/callback exempted from session-auth wrapper; state JWT enforces identity
- All token columns encrypted at rest; list/detail responses strip token columns
- bankStatementImporter encrypts counterpartyName, counterpartyIban, rawPayload; dedups via UNIQUE constraint
- syncBankConnection is the single source of truth called by both worker and POST /:id/sync
- Worker Phase B no longer contains inline 3-day window; 90-day first-sync delegated to service
- Phase C consent-expiry warning untouched
- 3 new test cases + existing Plan 01/02 tests all pass
- pnpm --filter @rentular/api lint passes
- pnpm --filter @rentular/api build succeeds (tsup bundle)
</verification>

<success_criteria>
- pnpm --filter @rentular/api test --run exits 0 with all Plan 01/02/03 test files included
- pnpm --filter @rentular/api lint exits 0
- pnpm --filter @rentular/api build exits 0
- All MUST_HAVES truths in frontmatter are TRUE
- A manual curl against the dev server (POST /api/v1/bank-connections/institutions?country=BE) returns 503 when PONTO_CLIENT_ID is empty, or 200 with a list when configured
</success_criteria>

<output>
After completion, create .planning/phases/09-psd2-bank-connection-flow-api-routes-ui-for-landlords-to-con/09-03-SUMMARY.md listing: 8 endpoints with HTTP methods, services created, worker delta, test counts, sanitization fields stripped from responses.
</output>
